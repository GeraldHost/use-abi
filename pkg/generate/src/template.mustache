// Generated by use-abi
import { BigNumberish } from "ethers";
import { Contract } from "@ethersproject/contracts";
import { useCall, useSend, ZERO_ADDRESS } from "@useabi/internal";

import ABI from "./abi.json";
import ADDRESSES from "./address.json";

const contract = new Contract(ZERO_ADDRESS, ABI);
{{#view}}

export function {{strings.hookName}}({{strings.hookArgs}}) {
  return useCall<[{{strings.typeArgs}}], {{strings.retTypeArgs}}>(ADDRESSES, contract, "{{strings.method}}", [{{strings.useCallArgs}}]);
}
{{/view}}
{{#send}}

export function {{strings.hookName}}() {
  return useSend<[{{strings.typeArgs}}]>(ADDRESSES, contract, "{{strings.method}}");
}
{{/send}}
